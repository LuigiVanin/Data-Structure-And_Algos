#ifndef CPP_ALGO_UTILS
#define CPP_ALGO_UTILS
#include <stdbool.h>
#include <stdlib.h>

void swap(int *v, int i, int j);
bool is_sorted(int *array, int size);
void shuffle(int *array, int size);
void print_boolean_areray(bool *array, int len);

#endif
